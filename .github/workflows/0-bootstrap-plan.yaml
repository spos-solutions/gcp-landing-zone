name: '0-bootstrap-plan'

on:
  pull_request:
    branches:
      - production
    paths:
      - '0-bootstrap/**'
      - 'policy-library/**'
      - '.github/workflows/0-bootstrap-*.yaml'

permissions:
  contents: read
  pull-requests: write
  id-token: write

env:
  STAGE: '0-bootstrap'
  STAGE_DIR: '0-bootstrap/envs/shared'
  TF_VERSION: '1.3.0'

jobs:
  terraform-plan:
    name: 'Terraform Plan - Bootstrap'
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - id: 'auth'
        name: 'Authenticate to Google Cloud'
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER_NAME }}
          service_account: ${{ secrets.TF_SA_BOOTSTRAP }}
          access_token_lifetime: '3600s'

      - name: Terraform Init
        working-directory: ${{ env.STAGE_DIR }}
        run: terraform init

      - name: Terraform Validate
        working-directory: ${{ env.STAGE_DIR }}
        run: terraform validate

      - name: Terraform Format Check
        working-directory: ${{ env.STAGE_DIR }}
        run: terraform fmt -check -recursive

      - name: Terraform Plan
        id: plan
        working-directory: ${{ env.STAGE_DIR }}
        run: |
          terraform plan -input=false -out=tfplan
          terraform show -no-color tfplan > plan_output.txt
        continue-on-error: true

      - name: Comment PR with Plan
        uses: actions/github-script@v7
        if: github.event_name == 'pull_request'
        env:
          PLAN: "${{ steps.plan.outputs.stdout }}"
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const plan = fs.readFileSync('${{ env.STAGE_DIR }}/plan_output.txt', 'utf8');
            const output = `### Terraform Plan - Bootstrap 🏗️
            
            <details><summary>Show Plan</summary>
            
            \`\`\`terraform
            ${plan}
            \`\`\`
            
            </details>
            
            *Pusher: @${{ github.actor }}, Action: \`${{ github.event_name }}\`, Workflow: \`${{ github.workflow }}\`*`;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            });

      - name: Terraform Plan Status
        if: steps.plan.outcome == 'failure'
        run: exit 1

      - name: Upload Plan
        uses: actions/upload-artifact@v4
        with:
          name: bootstrap-tfplan
          path: ${{ env.STAGE_DIR }}/tfplan
          retention-days: 5
